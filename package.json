{
  "name": "remote-work-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && esbuild index.tsx --bundle --outfile=dist/app.js --jsx=automatic --define:process.env.API_KEY='\"$API_KEY\"'"
  },
  "dependencies": {
    "@google/genai": "^0.12.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "typescript": "^5.4.5"
  }
}
